meta {
  name: quiz-session
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/quiz-session
  body: none
  auth: bearer
}

headers {
  Range: quiz-session=0-10
}

auth:bearer {
  token: {{bearerToken}}
}
